---
title: Chute Architecture Map
status: Audit pass 1, 2026-09-24
owner: Founder
related_docs:
  - docs/ingestion-index.md
  - docs/architecture/README.md
  - src/app/room/chute.tsx
  - src/app/room/room-client.tsx
  - src/app/room/actions.ts
---

# Chute Architecture Map

Audit pass 1, taken on branch `claude/chute-architecture-audit-1c0svk` at 2026-09-24. The purpose is the coming refactor of the Chute brains: how the HomeRoom's main Chute and the per-row Drop ingest files, route them to an account, analyze them, and fan the resulting intelligence out across the surfaces that read it. The map is biased toward the ingest path and everything downstream of a filing.

Method: nine readers mapped the repo by responsibility, nine cite-checkers refuted each map line by line against the files, two synthesis agents wrote the trace and the rankings, twenty skeptics attacked the refactor candidates, and a critic hunted for gaps. The ingest core was read by hand as well, and the thirteen test suites that pin it were run. Every cite below survived a line check. Claims that come from code shape rather than a run are marked [inferred].

## 1. Entry points and routes

- **Every page is a self-gating server component.** 20 of 25 pages and all 7 route handlers declare force-dynamic: src/app/room/page.tsx:81, src/app/accounts/page.tsx:41, src/app/groundwork/page.tsx:93, src/app/intranet/page.tsx:40, src/app/playbook/page.tsx:17. So revalidatePath only busts the router cache. Nothing is statically cached anywhere.
- **/room is the HomeRoom.** The wayfinder labels it so at src/components/app-wayfinder.tsx:37-43 and files /today, /, /pipeline and /intake under archived surfaces at :86-122. Contradiction: /today still runs its full derivation at src/app/today/page.tsx:606-640 and every filing revalidates it at src/app/room/actions.ts:68.
- **The Chute mounts on exactly two pages.** /room hands it routingRoster() at src/app/room/page.tsx:793 and :842. /intranet hands it an inline roster with emails and domains only at src/app/intranet/page.tsx:121-138. The per-row Drop exists only inside RoomClient at src/app/room/room-client.tsx:532-545 and :1837. ActivityDock on /intranet is a third drop zone for the .csv alone at src/app/activity/dock.tsx:46-100.
- **The route handlers exist because server actions block navigation.** POST /intranet/run and /intranet/read wrap runBrain and readCapture at src/app/intranet/run/route.ts:15-18 and read/route.ts:12-16. GET /activity/evidence is the one door for staged bodies at src/app/activity/evidence/route.ts:1-12.
- **Middleware guards two paths by cookie presence.** src/proxy.ts:4-11 delegates to src/lib/access-proxy.ts:20-33, which redirects only "/" and "/prospect-field". Every other page calls getAppAccess at src/lib/auth.ts:130-186 itself.
- **room/actions.ts is the filing spine.** Imported by chute.tsx, room-client.tsx, today/actions.ts:21 and groundwork/actions.ts:15. Its refresh() at :65-71 revalidates /room, /accounts, /today, /groundwork and /, and never /intranet, /playbook or /sendbook.

Weight: src/app/room/page.tsx (the HomeRoom loader and the only mount of RoomClient), src/app/room/actions.ts (the spine), src/app/room/chute.tsx, src/app/room/room-client.tsx (the Drop lives inside the largest client file in the app), src/app/intranet/page.tsx (the second Chute mount with its own roster).

## 2. The data layer

- **The schema is mostly dead.** Fifteen Prisma models between prisma/schema.prisma:268 and :766 have zero prisma usage in src (TerritoryAccount, SourceEvidence, Note, PermissionHistory, InternalUnknown, CSMPartner, PEO, PEOClient, Opportunity, FollowUpPromise, BoundaryRule, DiscoveryFramework, PitchAsset, DailyServe, HmlClassification). The live record is AccountNote (:1092), Todo (:1053), AccountDisposition (:1151), Touch (:1031), plus TaskDone, SignalSnooze, PartnerNote, DashCard, PeoState, DemoNote and the Intranet tables (:1174-1254).
- **AccountNote.accountId is a namespaced key-value store.** The only guard is isNamespacedAccountId, a colon test, at src/lib/today/overlay.ts:111-113. The schema comment at :1093 says "book account id"; overlay.ts:104-113 says otherwise.
- **One sanctioned note writer, three bypasses.** createAccountNoteRow at src/lib/notes/write.ts:26-58 has 28 call sites. src/lib/activity/run.ts:190 and :208 create rows bare, with kind "mine", source "activity", and no lane, actors or recipients. src/app/accounts/draft-actions.ts:259-266 creates the template:mail row bare too, but sets its own provenance.
- **Todo has no writer at all.** Nine sites call prisma.todo.create directly and assemble the in-band tag codec by hand: src/app/room/actions.ts:522, :791, :1312, :1498; src/app/today/actions.ts:889, :1146; src/app/today/sheet-actions.ts:135; src/app/accounts/act-actions.ts:198; src/lib/today/mirror.ts:25. The codec lives at src/lib/today/route-notes.ts:21-42 and :85-128.
- **AccountDisposition is overloaded the same way.** Real dispositions on account ids at src/app/today/actions.ts:758-768 share the table with about seventeen marker families that reuse status "parked" so loadDispositions keeps them, per src/lib/room/bind.ts:15-24 and overlay.ts:224-232. The pastehash marker breaks the pattern: stampPasteMark writes status "filed" at src/app/room/actions.ts:124-126, which loadDispositions drops. The guard works only because roomPaste reads it by findUnique at :204-206 and undo deletes by prefix at :881-887.
- **Two read paths.** loadAccountNotes at overlay.ts:115-174 is the wide one for 14 callers: it canonicalizes aliased ids at :153 through src/lib/book/merge.ts:65-74 and infers actors and lane for legacy rows at :148 and :159-162. Forty-two narrow prisma.accountNote reads in twelve files bypass it, none applying the alias fold: eleven in activity/run.ts, eight in room/actions.ts, five in intranet/runners.ts.
- **The hide key is split.** /room writes hide:note:<id> at src/app/room/actions.ts:1417, and room/page.tsx:159, groundwork/page.tsx:195, sendbook/page.tsx:84 and pipeline/collect.ts:88 read that. /today writes hide:acct:<id> through hideKeyFor at src/lib/today/ledger.ts:74-77 and reads that at today/page.tsx:1094. A note hidden on one surface stays visible on the other.
- **Seeds and their overrides.** peos is static book.json plus OFF_BOOK, filtered and renamed at module load at src/lib/book/index.ts:70-73. contacts.json is server-only through src/lib/book/contacts.ts. research.json is overridden per account by research:<id> on Accounts at accounts/page.tsx:245-315 but only as a book-wide stamp on Groundwork at src/lib/groundwork/day.ts:224-234 and :516-552.

Store namespaces the ingest path writes, and who reads them:

| Namespace | Written by | Read by |
|---|---|---|
| `<accountId>` notes | roomPaste at actions.ts:408-446, archive note :343-370, outcome marker :588-594 | loadAccountNotes; every page |
| `pastehash:<acct>:<fp>` (disposition) | stampPasteMark actions.ts:115-137 | roomPaste :204-206, roomPasteUndo :881-887 only |
| Todo (accountId set) | absorbRead actions.ts:522-536 | buildAccountSheet sheet-view.ts:142-190; intranet mirror runners.ts:168-182 |
| `gaps:<id>` | fileGaps gaps.ts:61-89 from absorbRead :546-552, roomResearch :1114-1121, roomGapsRefill :1228-1232 | readGaps gaps.ts:40-59 on /room :486-488 and pipeline/collect.ts:100; never the brain |
| `playbook:market`, `playbook:lessons` | filePlaybook store.ts:101-135 from absorbRead :557-579 and playbook/actions.ts:16-36 | readPlaybook store.ts:79-97; room/actions.ts:1161-1176 directly; ingestPlaybook runners.ts:463-490 |

Namespaces other doors own: `research:` (deep-research.ts:16, double-written at actions.ts:1096-1110), `seat:` and `actdraft:` (act/lane.ts:14-15, written by act-actions.ts:43-226), `sendbook:` (sendbook/read.ts:15, written by groundwork/actions.ts:78-84), `wire:` and `inst:` (groundwork), `scratch:pad`, `pipeline:`, `template:mail`, and the second record's `activity:`, `gems:`, `support:`, `intent:`, `activity:stage:`, `activity:manifest` (activity/stores.ts:14-19). `risk:` and `inst:` are read at accounts/page.tsx:94-100 and groundwork/page.tsx:167-168 but nothing in src writes them.

Weight: src/app/room/actions.ts (the only ingest writer of five stores), src/lib/today/overlay.ts (every wide loader and the read-time inference), src/lib/notes/write.ts, src/app/today/actions.ts (17 marker upserts, 18 Touch writes, 13 Todo writes, no shared helper), src/lib/activity/run.ts (the parallel writer).

## 3. Ingest, browser side: parse and read

- **One reader, both doors.** readFileToText at src/app/room/read-file.ts:49-144 is called by the Chute at chute.tsx:356 and the Drop at room-client.tsx:535. It dispatches on readerFor at src/lib/paste-files.ts:509-520: .eml, .vtt, .txt, .xlsx, .docx and .msg parse in the browser; PDFs and images go to the injected server action. It refuses any result under 20 characters at :138-139, and roomPaste refuses again at the same floor at actions.ts:194-195.
- **The two PDF wrappers are thin.** roomReadPdf at src/app/room/actions.ts:1725-1735 binds the row first; chuteReadPdf at :1739-1746 gates on write only. Both call the one transcribePdf at :1663-1722, which asks only for OUTLOOK THREAD or TEAMS THREAD heads at :1700 and slices to 60,000 at :1718. So a PDF of a call transcript files as dialect SF with the 60,000 cap and no archive note [inferred from the branch structure at actions.ts:166-174 and :180].
- **The head-token contract has no shared constant.** Producers: emlToPaste paste-files.ts:196, msgToPaste :478, vttToPaste :309, transcriptDocToPaste :446, sheetToPaste :529, read-file.ts:93 for DOCUMENT, the transcriber, and the Outlook bookmarklet at src/app/intake/capture-shelf.tsx:33. Consumers each carry their own regex with a slightly different alphabet: the roomPaste dialect sniff at actions.ts:166-174, sniffPaste at paste-files.ts:12-34, the parseSfTimeline refusal at src/lib/sf-timeline.ts:271-272, ai-clean's SHAPED_HEAD and ctHunt at src/lib/intel/ai-clean.ts:327-328 and :367, intranet normalize at src/lib/intranet/normalize.ts:37-62, meeting.ts:28, extract.ts:54, and groundwork signals.ts:107. SPREADSHEET and DOCUMENT heads have no consumer and file as SF. TEAMS CHAT is accepted by sniffPaste and ai-clean but not by roomPaste at :168, and no producer emits it.
- **The fingerprint contradicts its own comment.** pasteFingerprint at paste-files.ts:42-53 hashes the whole normalized paste including the head line. The comment at :37-43 says a re-copy of the same thread dedupes, but the bookmarklets stamp a fresh date into the head at capture-shelf.tsx:33, and the .vtt and .docx of one recording carry different heads and bodies at :298-316 and :443-456.
- **.csv means two things by door.** probeActivityReport at src/lib/activity/upload.ts:14-24 is a separate parser run by the Chute at chute.tsx:352-355 and the dock at dock.tsx:46-63. The Drop never probes; DROP_ACCEPT lists .csv and readerFor maps it to "text" at paste-files.ts:502-505 and :518, so an activity export dropped on a row files as an SF-dialect note. This contradicts DROP_ACCEPT's "never disagree" comment at :502-503.
- **The rules fallbacks read a different grammar.** parseSfTimeline at sf-timeline.ts:266-272 refuses OL, TM and CT heads. rulesRead at src/lib/intel/rules-read.ts:259-276 reads the Outlook block and Teams turn grammar, not the head. A Teams chat gets kind "call" from the model at ai-clean.ts:141 but "email" from the rules at rules-read.ts:169. smartPaste in src/lib/paste.ts is wired only to /today and /notes at day-sheet.tsx:25 and notes-client.tsx:11; no onPaste exists in room-client or chute.
- **Coverage gap.** paste-files, rules-read and sf-timeline have suites. read-file.ts and transcribePdf have none.

Weight: src/lib/paste-files.ts (every file head producer, the sniff, the fingerprint, DROP_ACCEPT), src/app/room/read-file.ts (the one dispatcher and the only place a new file type is wired), src/app/room/actions.ts:1663-1750 (the transcriber and its two doors), src/lib/sf-timeline.ts (first fallback and the shared scrubber), src/lib/intel/rules-read.ts (the keyless fallback).

## 4. Ingest, server side: route, guard, file, fan out

- **roomPaste is the one server pipeline.** src/app/room/actions.ts:139-471 has exactly two call sites: fileTo at chute.tsx:218 and filePaste at room-client.tsx:490. The Drop's filePaste is also the door for the paste pane at :602-606, compose-box text that readsRich at :394-405, and the mismatch force button at :1994.
- **The order inside.** bindAccountId against the static peos array at :162 through src/lib/room/bind.ts:28-36. Dialect sniff at :166-174. Cap 400,000 for CT else 60,000 at :180. Duplicate guard by findUnique on the pastehash marker, failing open, at :198-229. Early judgeFiling with an empty claim unless force at :233-254. aiCleanTimeline or readFailed at :256-275. parseSfTimeline fallback at :276-279. rulesRead for non-CT stampless reads at :281-303. Late judgeFiling with the read's accountName unless force at :303-331. One createAccountNoteRow per entry, at most 40, at :408-446, with the glyph head, lane from laneFor, actors and recipients from provenance, source outlook|teams|call|salesnav|sf plus "-ai", and createdAt at the entry's own day. The CT archive note at :343-370. stampPasteMark at :454. absorbRead whenever the read object is non-null at :455-457.
- **The router has two callers on opposite sides of the wire.** routeCapture at src/lib/route-capture.ts:129-207 scores email 100, domain 80, person 75, name or alias 70, head token 55, initials 50, and auto-routes only above 60 with a 20-point gap at :97-98. The Chute calls it in the browser at chute.tsx:361 on the filename and :366 on the text. judgeFiling at src/lib/intel/misfile.ts:66-160 calls it on the server twice per verdict, at :80 over the roster and :93 over the bound row. A row with own evidence at or above 55 that strictly outranks every rival passes at :112-123; rung 1 objects on a claim failing accountMatches at :136-143; rung 2 objects on any rival at or above 55 at :147-157. A Chute filing routes the same text five times.
- **The roster is a static seed with two importers.** routingRoster at src/lib/book/roster.ts:21-46 memoizes emails, domains, people keys and AKA aliases from book.json, contacts.json and merge.ts. Its header at :1-8 claims one build for every door including the Intranet; the only importers are room/actions.ts:14 and room/page.tsx:67.
- **The guard blocks.** misfile.ts:40-41 and actions.ts:309 both say the verdict never blocks. roomPaste returns ok:false with filed:0 on either pass unless opts.force, at :241-254 and :319-331. Force skips both rungs but not the duplicate guard or the read, at :204-229, :233 and :310-311, so every forced filing is a second model spend on the same text.
- **absorbRead's four limbs.** Todo rows for owner "me" only through a direct prisma.todo.create, deduped by knowledgeKey against open todos, remindAt set to the due date at noon UTC, at :493-535. gaps:<id> through fileGaps with an inline known-set at :546-552 into src/lib/room/gaps.ts:61-89. playbook rows through filePlaybook with another inline known-set at :557-579 into src/lib/playbook/store.ts:101-135. An outcome marker note at :582-597 whose id is never returned. read.signals and owner "them" actions are dropped. absorbRead runs whenever aiCleanTimeline returned without throwing, even when the entries came from the rules fallback, because the read object stays set at :264-279 while `how` flips to rules.
- **Undo is partial.** roomPasteUndo at :858-903 deletes only the returned note ids on the bound account and the pastehash marker. It leaves the opened todos, the gap rows, the playbook rows and the outcome marker. roomPaste never calls mirrorNoteToSheet; only roomLog at :93 does.

### Do the Chute and the Drop share a code path?

They share the server entirely and fork on the client. Shared: readFileToText, roomPaste, roomPasteUndo, githubArchiveGrant and archiveFileToGitHub. Forked:

- Routing runs only in the Chute at chute.tsx:361-376. The Drop is pre-bound to row.accountId at room-client.tsx:488-490 and its only routing check is judgeFiling inside roomPaste.
- Files per drop. The Chute fires one un-awaited swallow per file under one batch id at chute.tsx:383-387. The Drop reads the FIRST readable file and vaults every other file unread at room-client.tsx:581-600. Two .eml files dropped on a row file one and vault the other. The Chute files both.
- The .csv probe and the second-record drive loop exist only in the Chute at chute.tsx:286-343.
- The vault grant is memoized per session in the Chute at chute.tsx:243-256 and fetched fresh per call in the Drop at room-client.tsx:556-560.
- Unreadable files. The Chute routes them by filename and vaults with no guard at chute.tsx:357-365. The Drop archives `unreadable` at once at :596-597, then passes the whole `files` list as `waiting` at :598, and filePaste archives `waiting` again on accept at :498-500, as does readDroppedFile on a failed read at :538-540. Every non-primary file is PUT twice; archive.ts:117-128 folds a timestamp on collision, so two copies land.
- Vault timing on readable files is the same at both doors, after roomPaste returns ok, at chute.tsx:219 and room-client.tsx:498-500. The Drop's "vault waits on the guard" comment at :591-599 fails on its own busy branch: a readable file dropped while pending or reading is archived unguarded at :599.
- Mismatch resolution. The Chute collapses "no sure match" and "guard objected" into one picker whose every choice files with force=true at chute.tsx:555-579 and :586-600, so nothing is re-judged. The Drop holds text and files in a two-sided banner whose force button re-runs the whole read against the same row at room-client.tsx:1974-2013.
- Chute picker fall-through [from code shape, not exercised]: fileTo patches a mismatch without text at chute.tsx:233-234, and text is patched only on the pick branch at :376. The select's `if (a && it.text)` at :591 therefore fails on an auto-routed file the guard rejected and falls to vaultTo at :600, so the readable text is vaulted under the picked account and never re-filed.
- Receipts. The Chute persists noteIds to localStorage per Chicago day at chute.tsx:76-135, so undo survives a reload. The Drop keeps them in component state at room-client.tsx:516-519. The Chute has a "dupe" state at :231-232; the Drop shows r.reason as a generic note at :527. Only the Chute has the batch-mate suggestion at :389-403.
- Race [inferred]: concurrent Chute swallows each pass the findUnique guard at actions.ts:204-208 before any of them stamps at :454. The Drop serializes on pending or reading.

Copy that contradicts the code: room-client.tsx:511-514 says "the account check did not run" on readFailed, but the early guard ran keyless and the late guard runs with an empty claim at actions.ts:233-254 and :310-317. room-client.tsx:513 says "cross it out and drop it again", but cross-out writes a hide:note: disposition at :1417 and leaves the pastehash, so the re-drop returns "Already on file" at :202-226. chute.tsx:6-7 says the guard runs "when the key is on", but the early rung is rules-only.

Weight: src/app/room/actions.ts (every ingest verb and every fan-out write, about 40 imports), src/app/room/chute.tsx, src/app/room/room-client.tsx, src/lib/intel/misfile.ts (the one guard, twice per filing), src/lib/route-capture.ts (the pure router at two layers).

## 5. The reads: model calls and fallbacks

- **Exactly one model read at filing time.** aiCleanTimeline at src/lib/intel/ai-clean.ts:348-351 and :359-393 sends the paste to claude-opus-5 under a JSON schema, 16,384 tokens with a fallback to 8,192, a 55-second timeout, one retry, and an extra commitment-hunt paragraph for CT heads at :366-369. Its only importer is room/actions.ts. sanitizeAiResult at :191-297 is the sole gate between model and storage. The result object is never stored.
- **Dead routing and stale doctrine.** modelFor voids its argument at :348-351, so the cheap-versus-strong routing comment at :321-343 decides nothing. The header at :1-2 calls this "the single LLM touchpoint" while thirteen modules call messages.create. "Opus or better, always" at :345-347 is contradicted by claude-sonnet-5 at src/lib/groundwork/wire.ts:147 and src/app/partners/actions.ts:140.
- **Everything rendered is re-derived from prose on every load.** corpusFor at src/lib/intel/extract.ts:97-218 and extractDealIntel at :256-432 regex-mine countries, products, headcount, timing, lastInbound and lastOutbound from the note bodies the model wrote. Direction is "out" when the actors sender matches MINE_RE or the head carries "— Antaeus" at :177-183. Direction is "in" only when attributed, not machinery, not a closer, and addressed to us, and the addressed-to-us test sits out whenever a caller omits homeSide at :157-160. The closer classifier from src/lib/intel/closer.ts runs at read time inside corpusFor at :165-166, never at filing.
- **Every other model call runs on demand, never inside roomPaste.** transcribePdf. runResearch at src/lib/intel/deep-research.ts:182-231 through roomResearch at actions.ts:1086, which writes research:<id> and a plain note at :1096-1109 and files asks to gaps at :1112-1121. mintAsks at src/lib/intel/ask-mint.ts:90-107 through roomGapsRefill at :1215. The draft desk at src/app/accounts/draft-actions.ts:166-173. The partners draft, a raw fetch that re-implements the health latch, at partners/actions.ts:126-150. The wire sweep at wire.ts:127-166. The second record's distill and refute at src/lib/activity/distill.ts:160-174 and :263-299. The intranet's runRead, runPlan, runSynthesis, runWorldAnswer, runVerdicts and runSplit.
- **A filed note is model-read twice by unrelated readers.** aiCleanTimeline at filing, then runRead after the intranet mirrors it at src/app/intranet/runners.ts:146-165 and :683-693 into src/lib/intranet/extract.ts:224-243, with a different output shape and no shared cache.
- **The health latch is the contract.** claudeClient and claudeAvailable at src/lib/claude/health.ts:30 and :95-104 serve 14 importers, and every other Available() gate aliases them. lexicon.ts welds redactMoney at :260, taken alone by 24 of 36 importers, to the extraction vocabulary at :6-56 and :136-220 that extract.ts:9-14, evidence.ts:8 and pipeline/build.ts:20-27 mine.

Weight: src/lib/intel/ai-clean.ts (schema, prompt, model policy, the only gate to the database), src/lib/intel/extract.ts (the read-time re-derivation every surface stands on), src/lib/claude/health.ts, src/lib/intel/lexicon.ts, src/app/room/actions.ts (three model calls and all filing-time persistence).

## 6. Derivation: the HomeRoom and Today

- **The /room chain, per card.** Seven stores load through overlay.ts at src/app/room/page.tsx:114-123. Then: hide:note: filter at :159, corpusFor with homeSide at :163-172, extractDealIntel at :173-176, relationshipFor at :212-215, cardNextStep and readOutcome at :232 and :241, lastTouchRead at :256-271, buildAccountSheet at :273-280, owedToMe and settledByRecord at :281-301, meetingRead and owedByThem at :313-348, readDeal at :350-411 into src/lib/room/engine.ts:199-272, suggestChecks at :418-423, readLoss at :456, readGaps at :486-488, peerQuestions at :493-501, and THEIRS from fetchSecondRecords at :521-545.
- **What a filing changes here.** The new notes appear in the record fold. Opened todos reach the TODAY register through buildAccountSheet at src/lib/room/sheet-view.ts:142-190, but a dated commitment sits in `delayed` until its noon-UTC remindAt passes at :155-164, because absorbRead sets remindAt at actions.ts:534. Gaps feed UNKNOWN through readGaps at gaps.ts:40-59. The outcome marker lights the loss read at src/lib/room/loss.ts:55-73. The court flips on lastInbound against lastTouch.
- **/today runs the same head with different inputs.** The corpus at src/app/today/page.tsx:653-664 has no hide filter and adds acct:<id> touches, then diverges into buildMorningBrief at src/lib/intel/brief.ts:108-112, a same-day ledger at :1086-1177 and open actions at :1283-1296. It never calls readDeal, lastTouchRead or buildAccountSheet.
- **Shared derivations.** relationshipFor at src/lib/intel/relationship.ts:17-31 with five callers. cardNextStep at src/lib/today/build.ts:366-389. isMeetingNote at src/lib/intel/meeting.ts:31, imported by touch.ts, day.ts, sendbook/read.ts and ask/live.ts. homeSideFrom, the "who is our side" roster, lives in src/lib/pipeline/build.ts:271-291.
- **Private recomputations.** Reply-owed is spelled five ways: engine.ts:199-272, brief.ts:108-112, pipeline/build.ts:517-521, day.ts:378-392, sendbook/read.ts:258-266. Last outbound has two definitions: src/lib/room/touch.ts:39-76 excludes self-addressed, meeting and future-dated notes; extract.ts:177-183 excludes none of those. Three "today" clocks sit on one /room row: morningDoneKey is UTC at build.ts:447-467, moveDoneKey is Chicago at bind.ts:22-24, the sheet zones are Chicago at sheet-view.ts:161. pipeline/build.ts:396-404 classifies meetings with its own regex and never calls isMeetingNote.

Weight: src/app/room/page.tsx (the whole chain inlined per card, reusable by nothing), src/lib/room/engine.ts, src/lib/today/overlay.ts, src/lib/intel/extract.ts, src/lib/today/build.ts (stage, done-key grammar and cadence that /room depends on).

## 7. Derivation: Accounts, Groundwork, Sendbook, Playbook, Act Lane

- **Every one splits the same map itself.** All are force-dynamic pages calling loadAccountNotes and walking it by key prefix: groundwork/page.tsx:147-204, sendbook/page.tsx:70-92, accounts/page.tsx:89-104 and :160-161 and :245 and :328, playbook/page.tsx:28-33. Nothing is pushed after a filing. /sendbook and /playbook are never revalidated by roomPaste, only by groundwork/actions.ts:95 and :143 and playbook/actions.ts:35 and :57, which force-dynamic reduces to a router-cache detail.
- **Groundwork reads a third corpus.** dealIntelFor at groundwork/page.tsx:237-251 gets notes stripped of actors, recipients and homeSide, minus salesnav and wire sources at :230-231. liveMotionIds at src/lib/groundwork/day.ts:78-106 excludes an account on intel.lastInbound within 21 days or an isMeetingNote within 14 days, and it runs on the UNFILTERED notes at :325 while intelById is filtered. isMeetingNote reads the source column and the body head at meeting.ts:31-51, never the glyph or lane. Seats are written only by forkAct at src/app/accounts/act-actions.ts:181-226, read only at groundwork/page.tsx:342-355, retired by recordSends over the raw map, and capped by SEAT_SLOT_CAP at day.ts:147 and :656-670. QueueInput at day.ts:120-143 lists every store the queue brain reads.
- **The Sendbook decides outbound by glyph and actors.** recordSends at src/lib/sendbook/read.ts:89-107 requires a glyph head and an actors sender matching MINE_RE and not a meeting, ignoring source and lane. warmDates and inboundDates at :118-149 exclude MINE_RE, CSM voices and auto-replies only, with no closer, machinery or addressed-to-us test. Groundwork builds the Sendbook without orgSignals at page.tsx:208-212; /sendbook builds it with them at sendbook/page.tsx:96-105, so lanes can disagree.
- **Accounts reads a different last touch.** LAST HUMAN TOUCH renders only the second record's rollup.lastHuman at accounts/page.tsx:345-352. /room's touch clock reads the first record and the touch log. Neither merges the other. actdraft:<id> is written by saveActDraft and consumed by fileActSend at act-actions.ts:43-122 and read only by Accounts; compose.ts:96-98 says the stage reads it, but groundwork/page.tsx:78 never imports it.
- **Playbook and the two surfaces no group covered.** readPlaybook at store.ts:79-97 renders the rows absorbRead and approveSecondDraft filed. /partners reads loadAccountNotes for relationshipFor and hands the whole map into the sonnet prompt at partners/page.tsx:107-131 and partners/actions.ts:91-112. applyPlay on the Accounts sheet reads notes for relationshipFor and writes PeoState.nextAction at src/app/book/actions.ts:95-114, so a filing changes the name baked into the archived pipeline's durable next action.
- **Four spellings of "the operator's outbound"** at touch.ts:39-76, sendbook/read.ts:89-107, extract.ts:174-183 and act-actions.ts:105-112. Three spellings of "they wrote to us" at extract.ts:158-186, sendbook/read.ts:114-149 and activity/rollup.ts:123-157.

Weight: src/lib/groundwork/day.ts (twelve rules, exclusions, seat cap, carryover), src/app/groundwork/page.tsx (the heaviest loader), src/lib/sendbook/read.ts (the outbound spelling that Groundwork, Accounts and the second record's acted sweep also import), src/app/accounts/page.tsx, src/lib/intel/meeting.ts.

## 8. The Intranet brain and the second record

- **Pull, never push.** roomPaste imports no intranet module. A filing enters the brain only when a sweep pass of runBrain, reached through POST /intranet/run at run/route.ts:15-18 and runners.ts:1630-1644 and :1682-1696, runs syncApp over the newest 300 AccountNote rows at src/app/intranet/runners.ts:135-167. mirrorAccountNote at src/lib/intranet/mirror.ts:78-110 skips namespaced ids at :91-93, strips the glyph head at :50-55 and seals through redactMoney(scrubSecrets) at :66-68. upsertDocs dedupes by checksum at :326-365; extractPending reads it at :595-623 on that pass or a later one. Each opened Todo is mirrored as its own document at :168-182. Playbook rows arrive through ingestPlaybook at :463-490. gaps: and research: never arrive, though doctrine.ts:92-93 declares those origins.
- **The trigger is human-only.** On open, catchUp(true) fires only when queue.pending is positive at src/app/intranet/intranet-client.tsx:361, and pending counts unread intranetDoc rows at runners.ts:1744-1759, so a fresh AccountNote raises nothing. The ⟳ button at :863 sweeps. The post-send-it catch-up at :311 runs with sweep=false and skips the mirror. There is no cron, no vercel.json, no instrumentation file.
- **A third intake shares nothing with the Chute.** The Intranet's "Send it" box calls intranetCapture at src/app/intranet/actions.ts:103-245, which writes intranetDoc rows directly with rawChecksum dedupe and never files an AccountNote.
- **The second record is a fully forked ingest.** probe at upload.ts:14-24, uploadActivityReport streaming checksummed batches at :37-166, activityStage at src/app/activity/actions.ts:25-51, stageActivityBatch at src/lib/activity/run.ts:230-300, then the activityRun loop driven by both the Chute at chute.tsx:320-335 and the dock at dock.tsx:46-63. runActivityPass writes intent:, support:, gems: and activity: through replaceNote and writeManifestStore at run.ts:174-212, :695-710 and :793-823 with bare prisma create, update and delete. No createAccountNoteRow, judgeFiling, closer, provenance or pastehash. It reads the first record once, in the distiller's context pack via recordSends and inboundDates at :493-557, and the acted sweep re-stamps gems from recordSends at :1036-1056. Routing is by Salesforce id with SF_ID_OVERRIDES at ingest.ts:79-91.
- **Consumers and refresh.** fetchSecondRecords at src/lib/activity/read.ts:48-88 has seven importers: room/page.tsx:144-146 for THEIRS, groundwork/page.tsx:284 and day.ts, sendbook/page.tsx:96-105 for orgSignals, playbook/page.tsx:41-60 for the draft queue, accounts/page.tsx:222-224 for LAST HUMAN TOUCH and the act chip, and pipeline-actions. No revalidatePath exists under src/lib/activity or src/app/activity, and the Chute never calls router.refresh, so those surfaces change on next load. The take-back at run.ts:1122-1141 deleteMany's every namespace, and markVanished at runners.ts:372-391 never stamps the activity origin, so the brain keeps citing digests from drops that no longer exist.

Weight: src/app/intranet/runners.ts (the whole write half of the brain and the only mirror caller), src/lib/intranet/mirror.ts, src/lib/activity/run.ts, src/lib/activity/read.ts, src/app/intranet/actions.ts.

## 9. External edges and auth

- **Database:** getPrisma and hasDatabaseEnv at src/lib/db.ts:9, :18 and :22 over DATABASE_URL or DIRECT_URL. The "no env, return empty" guard repeats per loader at overlay.ts:18, :37, :116, :240 and :275.
- **Model:** ANTHROPIC_API_KEY and ANTHROPIC_WORKSPACE_ID at health.ts:21 and :90. Model ids are literals in nine files. partners/actions.ts:126-150 bypasses claudeClient with a raw fetch and no workspace header.
- **GitHub vault:** browser-side PUT on a short grant. githubArchiveGrant at src/app/room/archive-actions.ts:13-28 reads GITHUB_ARCHIVE_REPO and GITHUB_ARCHIVE_TOKEN behind canWrite. archiveFileToGitHub at src/lib/github/archive.ts:97-150 writes accounts/<Account>/<file>, folding a timestamp on collision; files over 25 MB draft a pre-release at :153-215. Nothing in the app reads the vault back.
- **Salesforce is not a live API.** src/lib/salesforce.ts:1-5 is URL builders and prefilled forms; SF_ID_OVERRIDES at :17-35 maps sixteen synthetic ids. The "Salesforce ingest" is the CSV second record. The bookmarklets at src/app/intake/capture-shelf.tsx:24-33, :98-109 and :130 copy to the clipboard and open /room or /intranet?grab=1; no text travels by URL, so a bookmarklet capture reaches roomPaste only by a hand paste.
- **Auth:** getAppAccess at src/lib/auth.ts:130-186 verifies an HMAC cookie with a 90-day window at :65-96, upserts the single User as OWNER on every call at :157-176, and derives canWrite as constant true at :182, all switched by PUBLIC_ACCESS at src/lib/public-access.ts:5. The middleware checks cookie presence at access-proxy.ts:21; pages check validity at auth.ts:125-128. The write gate is copy-pasted into fifteen action files, fourteen spelled one way and activity/actions.ts:19-23 negated. The operator's identity is defined twice: env-derived at auth.ts:55-57, hardcoded as OPERATOR_NAME and MINE_RE at src/lib/intel/provenance.ts:12-16.

Weight: src/lib/auth.ts, src/lib/db.ts, src/lib/claude/health.ts, src/lib/github/archive.ts, src/lib/salesforce.ts.

## 10. Centrality

| Module | Importers | Jobs | Verdict |
|---|---|---|---|
| src/lib/auth.ts | 43 | session mint and verify, access code, cookies, User upsert, canWrite, PUBLIC_ACCESS switch | multi |
| src/lib/db.ts | 40 | Prisma singleton, env probe | single |
| src/lib/book/index.ts | 39 | static seed, getPeo alias fold, derived lists | multi |
| src/lib/intel/lexicon.ts | 36 | redactMoney (24 importers take it alone), extraction vocabulary, country tables duplicated against flags.tsx | multi |
| src/lib/dashboard/stages.ts | 21 | stage vocabulary, SF picklist binding, checklist rule, migrate mechanics, BRIEFS copy | multi |
| src/lib/today/overlay.ts | 20 | wide loaders, read-time provenance inference, alias fold, namespace guard, disposition filter | multi |
| src/lib/intel/provenance.ts | 17 | operator identity, home-side tests, write-time stamping, read-time inference, name normalization | multi |
| src/lib/intranet/doctrine.ts | 17 | constants | single |
| src/lib/claude/health.ts | 14 | key-health latch, client factory | multi |
| src/lib/book/contacts.ts | 14 | contacts roster, routing people index | multi |
| src/app/today/actions.ts | 12 | 53 actions, the done() redirect, cross-imports roomCompose | multi |
| src/lib/today/build.ts | 12 | account scoring, cardNextStep, guidance copy, done-key grammar, roundup cadence | multi |
| src/lib/intel/digest.ts | 12 | static seed | single |
| src/lib/today/route-notes.ts | 11 | Todo body codec, detectTargets second router | multi |
| src/lib/intel/extract.ts | 10 | corpusFor classifier, extractDealIntel, dealIntelFor | multi |
| src/lib/notes/write.ts | 10 | one writer, bypassed three times | single |

Cites for the verdicts: auth.ts:65-96, :102-106, :108-123, :157-176, :182; lexicon.ts:6-56, :136-220, :260; stages.ts:1-4 against :120-130; overlay.ts:111-113, :115-174, :148, :153, :224-232; provenance.ts:12-16, :49, :169, :188, :207, :216; health.ts:20-51, :89-104; contacts.ts:27-31, :46-77; today/actions.ts:21, :50-55; build.ts:44-63, :93-122, :366-389, :447-479, :523-669; route-notes.ts:21-128, :209-268; extract.ts:97-218, :256-432, :434-442.

---

## Closer 1: the ingest path, drop to every surface

1. **Mount.** The Chute renders on /room with the full roster at room/page.tsx:793 and :842, and on /intranet with an emails-and-domains roster at intranet/page.tsx:121-138, so the person and alias rungs of the router cannot fire at the Intranet door. The Drop renders inside each row on /room, pre-bound to row.accountId at room-client.tsx:488-490.
2. **Entry.** Chute: one un-awaited swallow per file under a batch id at chute.tsx:383-387. Drop: the first readable file only, the rest vaulted unread at room-client.tsx:581-600.
3. **Second-record fork, Chute only.** A .csv whose first 4 KB fingerprints as the activity report at upload.ts:14-24 leaves the paste pipeline at chute.tsx:352-355, streams batches to activityStage, and loops activityRun to done at :320-335. The Drop files the same export as a plain-text SF note.
4. **Read, both doors.** readFileToText at read-file.ts:49-144. PDFs and images go through transcribePdf at actions.ts:1663-1722 via chuteReadPdf or the row-bound roomReadPdf.
5. **Unreadable, both doors.** Chute routes by filename and vaults unguarded at chute.tsx:357-365, or waits in "pick". Drop vaults immediately and again on the whole list at room-client.tsx:596-598 and :538-540.
6. **Route, Chute only.** routeCapture at route-capture.ts:129-207 on the text at chute.tsx:366; a best hit calls fileTo with force=false, else the item waits in "pick" with text and candidates.
7. **roomPaste, both doors.** Bind at actions.ts:162; dialect at :166-174; cap at :180.
8. **Duplicate guard** by findUnique on pastehash:<acct>:<fp> at :198-229; fails open; force cannot skip it.
9. **Early misfile guard,** rules-only, claim empty, at :233-254. Returns ok:false with the mismatch unless force.
10. **The one model read** at :256-275, then parseSfTimeline at :276-279, then rulesRead at :281-303.
11. **Late misfile guard** with the read's company claim at :303-331. A Chute filing has now routed the same text five times.
12. **Record writes** at :408-446: one note per entry with glyph head, actors, recipients, source and the entry's own date; the CT archive note at :343-370; a tail-trimmed transcript note when a non-CT read found nothing at :389-406.
13. **stampPasteMark** at :454 with status "filed", invisible to loadDispositions.
14. **absorbRead** at :473-603: todos for owner "me", gaps, playbook market and lessons, the outcome marker. Runs even when the entries came from the rules fallback.
15. **refresh()** at :65-71 revalidates /room, /accounts, /today, /groundwork and /. Nothing is pushed to the Intranet, /playbook or /sendbook.
16. **Receipt and vault.** Chute: on ok, vault the source file with the memoized grant at chute.tsx:209-236 and :243-280, patch the ledger row, persist to localStorage. Drop: on ok, archive `waiting` with a fresh grant, compose a prose receipt, spring TODAY open at room-client.tsx:488-529.
17. **Take-back, both doors.** roomPasteUndo at :858-903 removes the returned notes and the marker only.
18. **HomeRoom /room** re-derives on the revalidated request: record fold, TODAY via buildAccountSheet (dated commitments wait in `delayed`), UNKNOWN via readGaps, the loss read via the outcome marker, the court via readDeal, the pipeline drawer via collectPipelineAccounts at room/page.tsx:799-820.
19. **/today** and **/** re-derive on the revalidated request with their own corpora at today/page.tsx:653-688 and app/page.tsx:94-118.
20. **Accounts** re-derives on the revalidated request: chipNotes, relationshipFor, deskScore's live clock, engaged state at accounts/page.tsx:229-240 and :390-409. The second-record column does not move on a paste.
21. **Groundwork** re-derives on the revalidated request: dealIntelFor over the stripped corpus, liveMotionIds can drop the account from the queue, buildSendbook re-lanes, seats retire on recordSends, carryover clears on intel.lastOutbound, at groundwork/page.tsx:226-355 and day.ts:81-106 and :694-697.
22. **Sendbook** and **Playbook** re-derive on the next full request, not a revalidated one, at sendbook/page.tsx:63-105 and playbook/page.tsx:28-33.
23. **/partners** and **applyPlay** read the new notes on their next load or click at partners/page.tsx:107-131 and book/actions.ts:95-114.
24. **The Intranet** sees the filing only on the next human-driven sweep, through syncApp and mirrorAccountNote at runners.ts:135-167 and mirror.ts:78-110, then extractPending. gaps rows never arrive.
25. **The GitHub vault** receives the file in the browser and is never read back.
26. **Second-record consumers** (THEIRS, Groundwork rule fuel, Sendbook orgSignals, the Playbook draft queue, Accounts LAST HUMAN TOUCH, the Intranet digest) change only on a Chute .csv, on next load, with no revalidation anywhere under src/app/activity or src/lib/activity.

## Closer 2: the five files where a change breaks something far away

1. **src/app/room/actions.ts.** Four importers, but roomPaste is the sole author of the record's grammar: the glyph head, actors line and source literal at :408-446, the pastehash key at :202, and absorbRead's fan-out at :473-603. Nine read-time modules re-parse those strings: extract.ts:136 gates direction on the glyph, meeting.ts:34-35 reads the source literals, sendbook/read.ts:94-100 reads glyph and actors, touch.ts:49-53 reads actors, signals.ts:24 exists only if the source literal is "salesnav". The farthest reach is the second record's gems store: actedSweep stamps actedDay from recordSends over the account's notes at run.ts:1044-1056.
2. **src/lib/today/overlay.ts.** loadAccountNotes is the read contract of fourteen callers. isNamespacedAccountId at :111-113 decides what the brain may mirror at mirror.ts:18 and runners.ts:151. LOADED_DISPOSITION_STATUSES at :224-232 decides which marker families survive a reload for every page that prefix-filters them.
3. **src/lib/intel/extract.ts.** corpusFor is the app's one inbound-versus-outbound classifier, and extractDealIntel's lastInbound and lastOutbound sit behind /room's court at engine.ts:197-204, /today's reply-waiting at brief.ts:108-112, the pipeline at pipeline/build.ts:517-521, Groundwork's 21-day exclusion and drumbeat at day.ts:81-106 and :378-392, and the ask mint at actions.ts:1196. Seven call sites hand it seven corpora, so one rule change lands differently on each.
4. **src/lib/intel/lexicon.ts.** redactMoney runs on every model output at ai-clean.ts:213-216, every filed body at actions.ts:424, the archive at :353, every mirrored brain document at mirror.ts:66-68 and the second record's refuter context at run.ts:511-517. It is welded to the vocabulary that sets the meter: evidence.ts:8 imports countriesIn for suggestChecks, whose output positions the deal at engine.ts:128-185. A country-alias edit moves a stage read.
5. **src/lib/intel/provenance.ts.** MINE_RE and OPERATOR_NAME are hardcoded at :12-16 beside the env-derived owner at auth.ts:55-57. The same module stamps at write time through actorsLine and laneFor in roomPaste and infers at read time through inferActors in overlay.ts:148, extract.ts:140, sendbook/read.ts:95-99 and touch.ts:49-53. Its constant decides the Sendbook's lanes, Groundwork's drumbeat, and which gems get acted.

## What the tests pin

The verify chain's test script at package.json:10 runs six suites, none of which touch roomPaste, the Chute or the Drop. The other 61 suites exist only by hand. The thirteen that pin ingest were run on 2026-09-24 (paste-files, route-capture, misfile-guard, ai-clean, rules-read, sf-timeline, read-absorption, room-read, closer, vault, room-parity, second-record-faces, live-motion):

| Result | Count |
|---|---|
| tests | 313 |
| pass | 312 |
| fail | 1 |

The one failure is already stale on main: tests/read-absorption.test.ts:653-657 asserts a `RESEARCH ${new Date(row.researchAt)` literal, and room-client.tsx:951-954 now renders `RESEARCH ⟳`. Most of these suites source-scan text and order rather than behavior. misfile-guard.test.ts:111-154 slices room/actions.ts between the `roomPaste(` and `absorbRead(` literals and requires the early guard to precede the read; splitting roomPaste into a module or renaming absorbRead breaks it. vault.test.ts:74-82 and :92-98 pin BOTH `archiveFiles(unreadable)` and `archiveFiles(waiting)` as the decreed wiring, so the double-write in candidate 6 is a tested contract. room-parity, followups (:301-313 requires the `"TM"` and `"teams"` literals in room/actions.ts), ai-clean (:109-124 pins prompt sentences) and second-record-faces (:901-933 pins chute.tsx identifiers) all pin text. read-absorption and second-record-faces cannot load without a generated Prisma client because gaps.ts:7 and playbook/store.ts:13 import the writer, which imports db.ts.

## Closer 3: refactor candidates, ranked by blast radius

Eight survived two independent skeptics each; two were refuted and are listed last with the reason.

1. **The head-token and source grammar is authored once and re-sniffed in eight modules.** Overloaded. Producers at paste-files.ts:196, :309, :446, :529 and actions.ts:1700; consumers at actions.ts:166-174, paste-files.ts:12-34, sf-timeline.ts:271-272, ai-clean.ts:327-328 and :367, normalize.ts:37-62, meeting.ts:28, extract.ts:54, signals.ts:107; the stored source literal re-read at extract.ts:136, sendbook/read.ts:94, meeting.ts:34-35 and signals.ts:24. Blast: /room, /groundwork, /sendbook, /intranet and the read itself. Skeptics' correction: roomPaste is a consumer of the head, not its owner, and the intranet's normalize reads captures in parallel, not downstream, so the input side is two contracts with different radii. Shape: one dialect module exporting the head alphabet, one sniff, and the source-literal table.
2. **A filing's re-derivation is computed per surface from seven corpora, and "whose move" five ways.** Duplicated. corpusFor and extractDealIntel at room/page.tsx:159-176, today/page.tsx:653-673 and :743-744, app/page.tsx:94-107, pipeline/build.ts:360-370, groundwork/page.tsx:227-251, actions.ts:1183-1210, intake/actions.ts:77. Reply-owed at engine.ts:197-204, brief.ts:108-112, pipeline/build.ts:517-521, day.ts:378-392, sendbook/read.ts:258-266. Skeptics' correction: Groundwork recovers actors from the head through inferActors, so the actors line does reach it; the real divergence is homeSide, recipients, the hide filter and the source filter. Shape: one account read built per request with a declared homeSide and hide filter, and one whoseMove over it.
3. **absorbRead's fan-out is rebuilt at three other doors, and Todo has no writer.** Duplicated. The gaps known-set is built inline at actions.ts:546-552, :1114-1121 and :1228-1232 before fileGaps; the playbook known-set at :565-578 and playbook/actions.ts:23-35. Todo rows are created at nine sites with the tag codec assembled by hand. Skeptics' correction: drop the /playbook revalidation consequence since the page is force-dynamic, and the count of tagged Todo writers is five, not nine. Added by the critic: the intranet mirrors each opened Todo as a separate document at runners.ts:168-182, so the Todo shape is also what the brain reads. Shape: fileGaps and filePlaybook own their dedupe; a createTodoRow sibling of createAccountNoteRow carries the codec.
4. **Two routing rosters for one Chute component.** Forked. room/page.tsx:793 passes routingRoster() with people and aliases; intranet/page.tsx:121-137 rebuilds it inline without them under a comment at :118-120 claiming identical behavior. The people rung at route-capture.ts:14-17 exists specifically for call transcripts. Skeptics' correction: every picker choice files with force=true at both doors, so the Intranet does not uniquely bypass the guard; it reaches the picker more often, and each pick there skips the guard. Shape: delete the inline roster and hand both mounts routingRoster().
5. **The .csv door is forked three ways.** Forked. The Chute and the dock duplicate probe, upload, drive loop, receipt reconcile and the two-press take-back at chute.tsx:161-176, :286-343, :499-512 and dock.tsx:33-106. The Drop never probes and files the export as a note. Skeptics' correction: the early guard usually catches a multi-account CSV on a row with a mismatch banner, so the misfiled note needs a force; and the duplication is four pieces plus arming, not three. Shape: one shared swallowActivity hook, and the Drop probes or DROP_ACCEPT stops listing .csv as readable.
6. **handleFiles is forked, with a vault double-write.** Duplicated. Concurrency and grant handling differ at chute.tsx:383-387 and :243-256 versus room-client.tsx:556-560 and :581-600. Every non-primary file on a row is PUT twice at :596-598 and :498-500, and archive.ts:117-128 stamps a timestamp on collision so both copies land. Skeptics' correction: no surface reads the vault back, so the reach is the vault tree only; but the Drop loses record data, since a second readable file is vaulted and never filed. vault.test.ts pins the current wiring. Shape: one vault hook called after the same accept verdict, with `waiting` computed as files minus those already archived.
7. **Two receipt renderers and two undo memories for one roomPaste result.** Duplicated. chute.tsx:438-453 and :76-135 versus room-client.tsx:501-517 and :516-519. Skeptics' correction: the Drop does have an undo button at :1558-1566, so the working path exists; the defect is the readFailed copy that sends the operator to cross-out. One shared renderer is not achievable as written, because the row's receipt carries affordances the Chute cannot host. Shape: one receipt type fed by the result, with the ledger persistence lifted into a shared hook.
8. **Two PDF doors over one transcriber, and the transcriber never emits a transcript head.** Duplicated, small. actions.ts:1725-1735 and :1739-1746 differ by a bind roomPaste repeats at :162. Skeptics' correction: the prompt and cap already live in one function, so merging the doors saves almost nothing; the real fix is a prompt that emits CALL TRANSCRIPT for call recordings so the CT cap and archive note apply. Shape: one read-document action, bind left to roomPaste, prompt amended.
9. **Mismatch and pick resolution implemented twice with different force semantics.** Forked. The Chute's picker forces every choice at chute.tsx:555-579 and :586-600 and never re-judges; the Drop's banner re-runs the whole read against the same row at room-client.tsx:1974-2013. Skeptic A confirmed the fork; skeptic B refuted the blast as contained to two files, so this ranks below the candidates above. It carries the picker fall-through verified in section 4, which is a correctness bug on the Chute's auto-route path. Shape: one verdict component for both doors, plus a roomPaste option that re-judges against the picked account from a read cached by fingerprint.
10. **Three AccountNote writers bypass createAccountNoteRow.** Refuted by both skeptics on the same-job test: activity/run.ts:190 and :208 are replace-forward writers with an update path the append-only writer lacks, and draft-actions.ts:259-266 sets its own provenance and redaction. What stands is the fact: second-record rows never carry lane, actors or recipients, so provenance or redaction changes inside write.ts miss them, and the intranet digest at runners.ts:263-296 reads those rows.

Not a Chute candidate but worth knowing before touching copy: the hide:note: versus hide:acct: split in section 2 is what makes the Drop's "cross it out" advice fail twice, once because cross-out hides rather than deletes, and once because the two surfaces do not agree on which key hides a note.
